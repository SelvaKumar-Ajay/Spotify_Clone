class SearchSizes {
  double height;
  double width;
  SearchSizes({
    required this.height,
    required this.width,
  });

  late double iconheight = height * 0.04;
  late double containerheight = height * 0.07;
  late double containerwidth = width * 0.80;
  late double iconPaddingAll = width * 0.02;
  late double micIconHeight = width * 0.06;
  late double micIconWidth = width * 0.13;
  // late double iconPaddingAll = width * 0.02;
  // late double iconPaddingAll = width * 0.02;
}
