// Search tab Container Sizes
class ContainerSizes {
  double height;
  double width;
  ContainerSizes({
    required this.height,
    required this.width,
  });
  late double imgHeightWidth = width * 0.20;
  late double textPaddingLeft = width * 0.02;
  late double textPaddingTopRight = width * 0.03;
  late double crossMainAxisSpaces = width * 0.03;
  late double textSizedBox = width * 0.24;
  // late double containerwidth = width * 0.3;
  // late double containerwidth = width * 0.3;
  // late double containerwidth = width * 0.3;
  // late double containerwidth = width * 0.3;
  // late double containerwidth = width * 0.3;
}

//define sizes for Search bar and beside icon
class SearchSizes {
  double height;
  double width;
  SearchSizes({
    required this.height,
    required this.width,
  });
  late double browseAllPadding = width * 0.04;
  late double iconheight = height * 0.04;
  late double containerheight = height * 0.07;
  late double containerwidth = width * 0.80;
  late double iconPaddingAll = width * 0.02;
  late double micIconHeight = width * 0.06;
  late double micIconWidth = width * 0.13;
  // late double iconPaddingAll = width * 0.02;
  // late double iconPaddingAll = width * 0.02;
}
