class SongScreenSizes {
  double height;
  double width;

  SongScreenSizes(this.height, this.width);
  late double containerPadding = width * 0.04;
  late double icon = height * 0.04;
  late double titleText = height * 0.03;
  late double timingValues = height * 0.02;
  late double avatarRadius = height * 0.05;
  late double playPause = height * 0.06;
  late double spacebwImgTitle = height * 0.08;
  late double image = height * 0.50;
  // late double containerPadding = width * 0.04;
}
